# agent_wiki/runtime — 运行态工作层

跨 runtime 交接和尚未晋升的发现放这里。本目录的存在保证 `.omc/wiki`
符号链接在克隆后不悬空(git 不保存空目录)。

规则见 [../AGENTS.md](../AGENTS.md):session-log 是 churn 不是知识;
值得保留的内容通过**晋升**离开本层(写成 durable 页),不是原地提交。
